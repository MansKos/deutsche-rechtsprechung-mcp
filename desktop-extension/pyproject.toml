[project]
name = "deutsche-rechtsprechung-mcp-extension"
version = "0.1.0"
description = "Claude Desktop Extension: Deutsche Rechtsprechung durchsuchen"
requires-python = ">=3.10"
dependencies = [
    "mcp>=1.0.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
